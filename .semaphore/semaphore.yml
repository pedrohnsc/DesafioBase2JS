version: v1.0
name: Hello Semaphore
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Setup
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - npm install
  - name: 'Block #2'
    task:
      jobs:
        - name: 'Job #2'
          commands:
            - cd DesafioBase2 && npm test
